# Em dev o Vite faz proxy de /api -> http://localhost:3000 (ver vite.config.ts),
# então normalmente não é preciso setar nada. Para apontar a API manualmente:
# VITE_API_URL=http://localhost:3000/api/v1
